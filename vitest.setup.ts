/**
 * Подготовка окружения для юнит-тестов (подключается через setupFiles
 * в vitest.config.ts). Выполняется до загрузки самих тестов и, значит,
 * до первого импорта src/config/env.ts.
 *
 * Зачем: env.ts проверяет обязательные переменные схемой zod и при нехватке
 * вызывает process.exit(1). Файл .env не под git, поэтому в свежей рабочей
 * копии (git worktree) его нет и тесты падают ещё на импорте модулей.
 *
 * Порядок важен. Сначала читаем настоящий .env, если он есть, и только потом
 * подставляем заглушки через `??=` — то есть исключительно там, где значения
 * не оказалось. Если сделать наоборот, заглушки займут место первыми, а
 * dotenv по умолчанию не перезаписывает уже заданные переменные (override:
 * false) — настоящий .env разработчика перестал бы действовать, и тесты вели
 * бы себя по-разному у разных людей.
 */
import { config } from 'dotenv';

config();

process.env.BOT_TOKEN ??= '123456789:unit-test-token';
process.env.BOT_USERNAME ??= 'unit_test_bot';
process.env.SUPPORT_GROUP_ID ??= '-1001234567890';
// Несуществующая база: юнит-тесты подменяют Prisma заглушками и никуда не
// ходят, а если случайно пойдут — упрутся в ошибку подключения, а не в живые
// данные разработчика.
process.env.DATABASE_URL ??= 'postgresql://unit-test:unit-test@localhost:5432/unit_test_stub';
