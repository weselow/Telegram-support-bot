# Decisions: 005-topic-creation

## 2025-12-26: Архитектура

### Формат названия топика

**Решение:** `{first_name} ({user_id})`

**Причины:**
- Имя для быстрой идентификации
- ID для уникальности при одинаковых именах

### Формат карточки тикета

```
📋 Новый тикет

👤 Пользователь: {first_name}
🆔 Telegram ID: {user_id}
📱 Username: @{username} (если есть)
🔗 Источник: {source_url} (если есть)
📅 Создан: {created_at}

Статус: 🆕 Новый
```

### Архитектура сервисов

```
src/
├── db/
│   └── repositories/
│       ├── user.repository.ts    # CRUD для User
│       └── event.repository.ts   # CRUD для TicketEvent
└── services/
    ├── topic.service.ts          # Работа с Telegram Topics
    └── ticket.service.ts         # Бизнес-логика тикетов
```

### Обработка ошибок Telegram API

- Если не удалось создать топик → логируем ошибку, отвечаем пользователю о проблеме
- Если не удалось закрепить сообщение → продолжаем без закрепления (не критично)
