# Решения по задаче 013: SLA-таймеры

## Архитектура SLA

### Уровни напоминаний
- **first** (10 минут): `// ⏰ SLA: 10 минут без ответа`
- **second** (30 минут): `// ⚠️ SLA: 30 минут без ответа`
- **escalation** (2 часа): `// 🚨 SLA BREACH: 2 часа без ответа!`

### Формат сообщений
Все SLA-сообщения начинаются с `//` — это internal-сообщения, которые не пересылаются клиенту (фильтрация в support.ts).

### Логика таймеров
1. **Запуск**: при создании тикета или переоткрытии (status → NEW/REOPENED)
2. **Отмена**: при ответе сотрудника (любое сообщение в топике)
3. **Не перезапускается**: при новых сообщениях клиента (SLA считается от создания)

### Job ID формат
`sla-{userId}-{topicId}-{level}` — уникальный ID для отмены конкретного таймера.

## Реализация

### sla.service.ts
- `startSlaTimers(userId, topicId)` — планирует все 3 уровня
- `cancelAllSlaTimers(userId, topicId)` — отменяет все таймеры для тикета

### sla.worker.ts
- Проверяет что тикет не закрыт (status !== CLOSED)
- Отправляет сообщение в топик через bot.api
- Логирует SLA-события

## Интеграция
- `message.ts`: startSlaTimers после createTicket и при reopen
- `support.ts`: cancelAllSlaTimers при ответе сотрудника
